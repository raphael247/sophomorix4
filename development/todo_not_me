Ideen/Todo:
- linuxmuster-prepare umbenennen in turnkey (wie bei anderen appliances)
- loginprompt-farbe je server (server: rot)
- linuxmuster-setup
  extra script zum docker setup, und andere
- Passwortabfragen:
  Unterscheiden ob Passwort VERGEBEN wird ODER
  Passwort zum Zugriff auf Docker, ... BENÖTIGT wird
- user name nicht wiederverwenden
- Docker-container hash
  liste mit hashes auf die upgedated werden kann
  (nach überprüfung on der neue Docker container tut)


- default reverse proxy beim docker

Docker:

/srv/li../docker
docker stats
docker container ls


Firewall:
https://10.0.0.254/
root --> Muster!


  Schulungsangebot

   kurze technische Übersicht für Händler
   samba4
   ...
   Bugreports hier
   Quellcode dort
   Support, ...

   subnetting pro schule

   github wiki:
   Q&A
 


TODO:

create testusers.py
wie ermittelt ob command succesful war?
--> json auswerten
